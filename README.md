A registration form which is linked to a database called registration 
The database has a table knonw as users
The users table has 4 columns:
 - id primary key size(100)
 - username(100)
 - email(100)
 - password(100)
